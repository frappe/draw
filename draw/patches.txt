[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://docs.frappe.io/framework/user/en/database-migrations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated
# (App setup — Draw User role, permissions, "comment" permission type — now lives
# in draw.setup.ensure_setup, run from after_install + after_migrate so it also
# applies on fresh installs, which skip patches.)
# Back-fill: grant the Draw User role to existing System Users that pre-date the
# lazy grant on Draw access (#73). Idempotent, so it is safe on every migrate.
draw.patches.v0_0.grant_draw_user_role
# Repair: restore the doctype-declared perm rows that Draw's own Custom DocPerm
# rows switched off, leaving System Managers unable to create a diagram (#174).
draw.patches.v0_0.restore_declared_permissions
